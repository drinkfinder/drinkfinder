/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SavedIngredientsComponent } from './saved-ingredients.component';

describe('SavedIngredientsComponent', () => {
  let component: SavedIngredientsComponent;
  let fixture: ComponentFixture<SavedIngredientsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SavedIngredientsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SavedIngredientsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/